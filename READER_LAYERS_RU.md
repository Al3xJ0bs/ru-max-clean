# Дополнительные читательские словари

В публичной версии v1.2 доступны независимые companion-пакеты для чтения русской и переводной
литературы. Они не смешиваются с основным RU-Max-Clean: каждый слой имеет свои
файлы `.ifo/.idx/.dict` и может быть включён в KOReader отдельно.

## Пакеты

* `latin_classical` — распространённые латинские выражения и термины с русским
  объяснением и транслитерированными алиасами;
* `literary_archaic` — историческая, архаическая и церковнославянская лексика;
* `french_literary` — французские слова, формы обращения, титулы и выражения,
  часто встречающиеся в русской классической прозе;
* `literary_names` — персонажи, исторические лица и географические названия из
  проверенного литературного корпуса;
* `fantasy_terms` — термины и понятия, специфичные для фэнтезийных миров;
* `literary_terms` — исторические и культурно-специфичные термины из переводной
  и классической литературы;
* `phraseology` — устойчивые обороты, античные и библейские аллюзии.

Входной формат — UTF-8 TSV:

```text
слово<TAB>русское определение<TAB>алиас1|алиас2
```

Стартовые записи являются небольшой авторской seed-выборкой для проверки
формата и качества. Перед расширением источники необходимо проверять на
совместимость лицензий. Для массовых пополнений предпочтительны Wiktionary,
Wikidata и источники public domain/CC-compatible.

## Сборка пакетов

Из каталога builder:

```text
py -3 -X utf8 build_reader_packs.py --pack-dir reader_packs --output-dir RU-Reader-Packs
```

Каждый пакет проходит обычную StarDict-валидацию:

```text
py -3 -X utf8 validate_stardict.py RU-Reader-Packs\latin_classical\ru-max-clean
```

Французский слой намеренно не объединяется с латинским: короткие служебные
слова (`de`, `la`, `en`) могут иметь совпадающие ключи, но относятся к разным
языкам и должны подключаться читателем осознанно.

## Coverage scan

Чтобы не импортировать огромные иностранные словари вслепую, можно измерить
реальный дефицит по книгам:

```text
py -3 -X utf8 scan_book_coverage.py book.epub \
  --index path\to\ru-max-clean.idx \
  --output BOOK_COVERAGE.json
```

Сканер поддерживает TXT, HTML, EPUB и gzip-текст, распознаёт кириллицу,
латиницу, греческий и смешанную запись, а также проверяет короткие
многословные выражения (`a priori`, `status quo` и т. п.). Отчёт сортирует
неизвестные токены по частоте и сохраняет варианты написания.

## Политика качества

* Основной словарь остаётся русскоязычным и не получает массовых латинских
  или энциклопедических записей.
* Иностранные и архаические слои должны иметь явный язык/домен в метаданных,
  короткое русское объяснение и проверяемый источник.
* Автоматические алиасы добавляются только после проверки коллизий; исходная
  форма всегда сохраняется.
* Для каждого расширенного слоя формируются отдельные `PACK_INFO.json` и
  отчёт покрытия.
